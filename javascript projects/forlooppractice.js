const arr = ['a', 'b'];
for (const element of arr) {
    console.log(element);
}

const arry = ['hello', 'world'];
for (let i = 0; i < arry.length; i++)
{
    const elem = arry[i];
    console.log(elem);
}